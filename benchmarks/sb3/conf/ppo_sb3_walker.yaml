defaults:
  - ppo_sb3
  - logdirs
  - _self_

hydra:
  job:
    name: walker2d-ppo

# hyperparameters for Hopper are here:
# https://github.com/DLR-RM/rl-baselines3-zoo/blob/master/hyperparams/ppo.yml#L171
env_name: Walker2d-v4
log_interval_episodes: 100  # each episode is up to 1000 steps
